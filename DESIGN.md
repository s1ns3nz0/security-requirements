# 보안 요구사항 도출 플러그인 — 설계 문서

작성일: 2026-07-27
상태: 설계 확정, 구현 착수 전

---

## 1. 문제 정의

기존 생태계에는 **발견적(discovery)** 보안 도구가 많다. 코드를 읽고 취약점을 찾는다. 그러나
**규범적(prescriptive)** 도구는 비어 있다 — "이 서비스는 무엇을 지켜야 하는가"를 코드가 없어도
규정해주는 것.

컴플라이언스 단계에서 보안 요구사항을 정의해야 하는 순간, 현재는 사람이 프레임워크 문서를 읽고
수작업으로 tailoring한다. 이 도구는 그 과정을 자동화한다.

**포지셔닝**: 기존 security review 도구의 경쟁자가 아니라 **상류**다. 이 도구의 산출물
(`requirements.yaml`)이 리뷰 도구의 입력이 되면, "일반적 보안 점검"이 "이 서비스의 REQ-014
이행 검사"로 바뀐다. 취약점 스캐너가 멀티테넌시 경계 위반을 못 잡는 이유는 그것이 코드 패턴이
아니라 요구사항 위반이기 때문이다.

---

## 2. 설계 결정 요약

| # | 결정 사항 | 선택 | 핵심 근거 |
|---|---|---|---|
| 1 | 입력 표면 | 레포 스캔 → 빈칸 인터뷰 하이브리드 | 단일 경로로 그린필드/기존 서비스 양쪽 커버 |
| 2 | 카탈로그 조달 | NIST OSCAL 번들 + CSP는 자체 룰 | 환각 방지 + 라이선스 제약 동시 해결 |
| 3 | 도출 엔진 | 베이스라인 × 위협모델 교차 + 책임분리 | 커버리지(완전성)와 관련성(도출)을 둘 다 확보 |
| 4 | 책임 판정 | 배포모델 층 + 서비스 큐레이션 + 4분류 | "상속"은 사실이 아니라 주장. 증적과 함께 표기 |
| 5 | 기본 프레임 | CSF 2.0 목차 × 800-53 백엔드 | 개발팀과 감사인 두 독자를 한 문서로 |
| 6 | 규제 스코프 | v1 코어만 + 트리거 감지·미지원 선언 | 커버 못 하는 걸 커버한 척하지 않는다 |
| 7 | 산출물 | YAML SSOT + MD 렌더 + 추적표 | ID 안정성·재실행 diff·후속 도구 연결점 |
| 8 | 재실행 정책 | 필드 소유권 분리 + append-only 상태전이 | 사람 편집·예외승인 보존, 감사 이력 유지 |
| 9 | 패키징 | 명령 3개 + 스킬 + 결정론적 스크립트 | 조회는 스크립트, 판단은 모델 |
| 10 | 영향도 판정 | 데이터유형 테이블로 유도 + 게이트 확인 | 서비스 오너 어휘로 묻고, 산출 근거를 노출 |
| 11 | 위협 모델링 | DFD → 경계별 STRIDE + LINDDUN + 페르소나 | 일반론 억제. 서비스 고유 위협이 제품 차별성 |
| 12 | 검증 체계 | 단위테스트 + ID 빌드게이트 + 골든셋 커버리지 | 층마다 다른 검증 수단 |
| 13 | 큐레이션 | AWS 핵심 10개 수작업 + 나머지 모델 부트스트랩 | 최대 병목 해소. 신뢰도는 배지로 정직하게 구분 |
| 14 | 산출물 보안 | 정의/상태 분리 + 레포 가시성 기반 기본값 | 산출물 자체가 정찰 문서. 기본값이 안전해야 함 |
| 15 | 검증 경계 | verification 필드만, 자동검증은 v2 | 검증 로직 없이 검증 가능성만 확보 |
| 16 | 빌드 순서 | 트레이서 불릿 (최소 데이터 end-to-end) | 가장 큰 미지 = 도출 품질. 그것부터 확인 |

부가 결정:
- **출력 언어** — 입력 언어 추종. 산문 필드만 로케일, 기계 필드·컨트롤 원문은 영어 고정
- **라이선스** — 코드 Apache-2.0, 카탈로그는 디렉토리별 표기
- **면책** — 모든 산출물 상단에 자동 삽입

---

## 3. 파이프라인

| 단계 | 성격 | 담당 |
|---|---|---|
| 1. 레포 스캔 → 프로파일 초안 | 코드·IaC 해석 | 모델 |
| 2. 빈칸 인터뷰 (필수 7문항 이하) | 대화 | 모델 |
| 3. **프로파일 확인 게이트** | 승인 | **사람** |
| 4. 영향도 판정 → 베이스라인 선택 | 테이블 조회 | 스크립트 |
| 5. 위협 모델링 | 판단·창의 | 모델 |
| 6. 책임 분류 | 큐레이션 조회 | 스크립트 (미등록만 모델) |
| 7. 교차·우선순위 | 집합 연산 | 스크립트 |
| 8. 요구사항 작문 | 서비스 언어로 재작성 | 모델 |
| 9. 병합 | 필드 소유권 규칙 | 스크립트 |
| 10. 렌더 | 템플릿 | 스크립트 |
| 11. 품질 린트 | 규칙 + 판단 | 스크립트 + 모델 |

**3번 게이트는 생략 불가.** 프로파일이 틀리면 그 아래 전부가 정교하게 틀리고, 사용자는 역추적을
못 한다.

**4번을 모델에게 시키면 안 된다.** "Moderate 베이스라인에 뭐가 들어가나"는 판단이 아니라 조회다.
모델에게 맡기면 느리고, 비싸고, 실행마다 답이 달라지고, 없는 컨트롤이 섞인다.

### 도출 로직 (3단계)

```
profile
  ├─→ FIPS 199 영향도 → 800-53B 베이스라인
  └─→ DFD → STRIDE/LINDDUN → 위협 목록
            │
            ▼  교차
  threat ∩ baseline  →  고우선순위
  threat only        →  추가 요구사항   ← 이 도구의 존재 이유
  baseline only      →  유지, 저우선순위, 근거 기록
            │
            ▼  책임 4분류
  상속 주장 (증적 필요) / 공유 / 조직 정책 / 팀 구현
```

`threat only` 버킷이 비면 이 도구는 "베이스라인 필터링기"로 격하된다. 위협 모델 품질이 제품
차별성과 1:1로 연결된다.

---

## 4. 디렉토리 구조

```
.claude-plugin/
  plugin.json
commands/
  sec-req-init.md        # 1~3단계
  sec-req-build.md       # 4~11단계
  sec-req-refresh.md     # 재실행·병합
skills/
  deriving-security-requirements/
    SKILL.md
    references/
      profile-schema.md
      threat-modeling.md      # DFD·STRIDE·LINDDUN·페르소나 절차
      requirement-style.md    # 작문 3원칙
scripts/
  select_baseline.py     # 결정론적
  classify_resp.py
  merge.py
  render.py
  lint.py
  rebuild_catalogs.py    # OSCAL → JSONL 재생성
catalogs/
  nist-800-53r5/         # 공공도메인
    AC.jsonl ... (20 패밀리)
    baselines.json
  csf-2.0/
    subcategories.jsonl
    crosswalk-800-53.json
  asvs-5/                # CC BY-SA 4.0 — 격리
    LICENSE  NOTICE
    V1.jsonl ...
  data-types/
    classification.yaml  # 데이터유형 → C/I/A 기여도
  csp-rules/             # 자체 작성 + 출처 URL
    aws.md
responsibility/
  layers.yaml            # 배포모델별 거친 매핑
  services/
    aws-s3.yaml          # reviewed: true
    ...
golden/                  # 전부 합성 케이스
  b2b-saas-aws/
  mobile-backend/
  internal-admin/
  commerce-payments/
tests/
```

### 사용자 레포에 생성되는 것

```
.security-requirements/     # 민감 — 가시성에 따라 gitignore
  profile.yaml
  threats.yaml
  requirements.yaml
  status.yaml
  state.yaml               # 발급된 ID 대장
docs/security/              # 공개 무해 — 항상 커밋
  requirements.md
  traceability.md
  responsibility.md
```

---

## 5. 데이터 스키마

### 요구사항 레코드

```yaml
- id: REQ-DATA-ENC-REST-01      # 내용 기반, 순번 아님
  managed:                       # 도구 소유
    statement: "저장 시 PII는 KMS 관리 키로 암호화되어야 한다"
    rationale: "..."
    csf: [PR.DS-01]
    sources: [SC-28, SC-28(1), SC-13, ASVS-6.2.1]
    threat_refs: [T-07]
    responsibility: shared
      csp_part: "KMS 키 인프라 가용성"
      team_part: "SSE-KMS 활성화, 버킷 정책"
      evidence: "AWS SOC 2 CC6.1 + terraform state"
    verification:
      method: iac_inspect
      target: "aws_s3_bucket_server_side_encryption*"
      expect: "sse_algorithm=aws:kms"
      fallback_manual: "콘솔 > S3 > 속성 > 기본 암호화"
    priority: high
  human:                         # 도구 불가침
    status: exception
    exception:
      approver: CISO
      reason: "..."
      expires: 2026-12-31
  pending_review:                # 재실행 제안, 승인 대기
    statement: "..."
    why: "프로파일 PII 등급 상향"
```

**ID는 순번이면 안 된다.** 재실행에서 요구사항이 중간에 추가되면 아래 번호가 전부 밀리고,
기존 티켓·감사 증적·예외 승인 기록이 전부 엉뚱한 요구사항을 가리키게 된다.

**요구사항은 삭제되지 않는다.** `superseded_by` / `retired`로 상태만 전이하고 사유를 남긴다.
지난 분기 감사 보고서에 있던 요구사항이 사라지면 감사인은 반드시 이유를 묻는다.

### 언어 규칙

| 필드 | 언어 |
|---|---|
| `statement`, `rationale`, `fallback_manual`, 위협 시나리오 | 입력 언어 추종 |
| `id`, `responsibility`, `method`, `status` 등 enum | 영어 고정 |
| 컨트롤 ID·조문 | 영어 원문 (번역 금지 — 원문이 근거) |
| `target` 코드 식별자 | 원본 그대로 |

`profile.yaml`에 `locale`을 기록한다. 없으면 refresh가 다른 언어로 실행됐을 때 문서가 혼재로
썩는다.

### 위협 레코드

```yaml
boundaries:
  - id: TB-4
    from: order-svc
    to: postgres
threats:
  - id: T-07
    boundary: TB-4
    category: STRIDE:E
    novelty: service_specific     # generic이면 저가치로 표시
    persona: authenticated_tenant
    scenario: "테넌트 A가 order_id 순차증가를 이용해 B의 주문 조회 —
               인가가 소유자 대신 로그인 여부만 검사"
  - id: T-11
    category: LINDDUN:Linkability
    scenario: "비식별 분석 로그의 device_id로 재식별 가능"
```

STRIDE는 프라이버시 위협을 못 잡는다. 연결가능성·식별가능성은 6분류 어디에도 없다.
프로파일에 PII가 있으면 LINDDUN 축을 추가로 돌린다.

---

## 6. 요구사항 작문 3원칙

1. **검증 가능** — 충족 여부를 판정할 방법이 문장에 함축돼야 한다
2. **원자적** — 하나의 요구사항에 하나의 관심사. "암호화하고 로깅해야 한다" 금지
3. **속성 규정, 구현 지정 아님** — "TLS 1.2 이상"은 요구사항, "nginx `ssl_protocols` 설정"은
   구현 가이드. 섞으면 도구가 아키텍처를 강탈한다

`lint.py`가 모호어(적절한·충분한·필요시), 접속사로 연결된 복수 관심사, `verification` 누락을
기계 검사한다.

---

## 7. 검증 체계

| 층 | 대상 | 수단 |
|---|---|---|
| 1 | 결정론적 스크립트 | pytest 단위 테스트 |
| 2 | 출처 ID 무결성 | 링크체커 — 카탈로그에 없는 ID 참조 시 **빌드 실패** |
| 3 | 모델 판단 | 골든 프로파일 커버리지 채점 |

**층 2가 카탈로그 번들의 두 번째 이유다.** 환각을 예방만 하는 게 아니라 사후 검출까지 된다.
이게 없으면 존재하지 않는 강화항목을 만들어내도 아무도 모른다.

**층 3은 문장 일치를 요구하면 안 된다.** 표현은 매번 달라진다. "테넌트 간 객체 접근 통제"라는
주제가 어떤 문장으로든 다뤄졌는지를 본다. 재현율만 자동 측정하고 정밀도는 사람이 표본 검토한다.

골든셋은 **전부 합성 케이스**여야 한다. 오픈소스 배포이므로 `golden/`이 공개되고, 실존 서비스
프로파일을 넣으면 그 서비스의 아키텍처와 미구현 통제를 공개하는 셈이 된다.

---

## 8. 큐레이션 부트스트랩

서비스별 책임 매핑이 프로젝트 최대 병목이다. 서비스 하나당 컨트롤을 훑으며 판정하는 데 2~4시간,
40개면 2~4주. 그리고 CSP는 서비스를 계속 내놓는다.

**전략**: AWS 핵심 10개(S3·RDS·Lambda·API Gateway·ALB·Cognito·DynamoDB·ECS·SQS·CloudFront)만
손으로 검증(`reviewed: true`). 미등록 서비스를 만나면 모델이 판정 초안을 만들어 yaml로 캐시하고
`reviewed: false`로 표시한다. 다음 실행부터 조회로 재사용된다.

출력에서 두 상태를 시각적으로 구분한다.

```
[검증됨] S3       — SC-13 shared
[미검증] Bedrock  — SC-13 shared   ← 사용자 확인 권장
```

Q2·Q4의 환각 방지 원칙과 긴장 관계이지만, **긴장을 표면화하면 해결된다.** "커버리지 없음"보다도
"검증된 척하기"보다도 낫다. 검토된 yaml이 커뮤니티 기여 단위가 된다 — PR 하나가 서비스 하나.

v1 CSP는 AWS만. Azure/GCP는 `layers.yaml` 수준의 거친 커버리지만.

---

## 9. 라이선스

| 대상 | 라이선스 |
|---|---|
| 코드·스크립트·자체작성 룰 | Apache-2.0 |
| `catalogs/nist-*` | 미국 공공도메인 (명시) |
| `catalogs/asvs-5/` | CC BY-SA 4.0 + NOTICE (격리) |

CC BY-SA의 share-alike는 각색물에 전파되지 코드 전체를 감염시키지 않는다 (GPL과 다름). ASVS를
JSONL로 변환한 것은 각색물이므로 해당 파일은 CC BY-SA를 유지하되, 별도 디렉토리에 두어
"모음(collection)"으로 구성하면 나머지 코드는 Apache-2.0을 쓸 수 있다. **디렉토리 분리가
라이선스 설계다.**

번들할 수 없는 것:
- CIS Benchmarks — 사실상 재배포 금지
- PCI DSS — PCI SSC 저작권. 요구사항 번호 참조만
- CSP 공식 문서 — 저작권. 자체 작성 요약 + 출처 URL로 대체
- ISO 27001 Annex A, SOC 2 TSC — 저작권

**제품명에 "NIST"를 넣지 않는다.** 공공도메인 저작물 사용과 NIST 승인 인상을 주는 것은 다른
문제다.

---

## 10. 산출물 보안

`profile.yaml` + `threats.yaml` + `status.yaml`은 공격자 관점에서 완벽한 정찰 문서다. 아키텍처
지도, 데이터 저장 위치, 미구현 통제, 리스크 수용 항목과 만료일이 한 곳에 정리돼 있다.

**정의와 이행 상태는 성격이 정반대다.**

- `REQ-...: 저장 시 PII 암호화` — 팀이 봐야 한다. 공개돼도 무해하다
- `REQ-...: status=open, 예외승인 2026-12-31` — "여기 아직 안 막혔고 연말까지 안 막을 예정"

`init`이 `gh repo view --json visibility`로 가시성을 감지한다. PUBLIC이면 민감부를
`.gitignore`에 자동 추가하고 사내 별도 저장소를 권고한다. PRIVATE이면 커밋을 기본으로 하되(감사
이력에 유리) 공개 전환 위험을 명시한다. 사용자가 뒤집을 수 있되 **기본값이 안전한 쪽**이어야 한다.

git 히스토리는 파일을 지워도 남는다.

---

## 11. 규제 미커버 선언

v1은 규제 오버레이를 포함하지 않는다. 대신 트리거를 감지해 정직하게 선언한다.

```
## 미커버 규제 감지

감지: payments/ 디렉토리, stripe SDK, card_last4 컬럼
→ PCI DSS 4.x 대상으로 보임. 본 도구 미지원, 별도 검토 필요

감지: resident_reg_no 컬럼
→ 개인정보보호법 고유식별정보. 미지원, 별도 검토 필요
```

`overlays/` 디렉토리는 스키마만 두고 비워둔다. 두 번째 카탈로그는 코어가 실제로 쓰이는 것을
확인한 뒤 붙인다.

**참고**: 한국 법령·고시는 저작권법 제7조에 따라 보호 대상이 아니므로 번들 가능하다
(개인정보보호법, ISMS-P 인증기준 고시 별표). 단 KISA 해설서는 별개 저작물. 향후 오버레이
1순위 후보.

---

## 12. 면책 (모든 산출물 상단 자동 삽입)

> 본 문서는 자동 생성된 초안이며 법적 자문이나 컴플라이언스 인증을 대체하지 않습니다.
> 유자격 검토가 필요합니다.

산출물 하단에 사용한 카탈로그와 버전을 자동 명기한다. 카탈로그 버전은 `profile.yaml`에 기록하고,
`rebuild_catalogs.py`로 재생성 후 diff를 검토한다.

---

## 13. 빌드 순서

가장 큰 미지는 데이터 분량도 아키텍처도 아니라 **모델이 실제로 쓸만한 요구사항을 쓰는지**다.
그게 안 되면 큐레이션에 쓴 시간이 전부 매몰비용이 된다.

### Week 1 — 트레이서 불릿

- 카탈로그: SC·AC·AU 3패밀리만
- 서비스: `aws-s3`, `aws-rds` 2개만
- 골든셋: 1개
- `init` → `build` → `render` end-to-end 관통
- **문서 실물을 보고 도출 품질 판단**

### Week 2 — 분기

- 품질 OK → 20패밀리 확장, CSF crosswalk, ASVS
- 품질 NG → 위협 모델링·작문 설계 재검토 (매몰비용 최소)

### Week 3+

- 서비스 큐레이션 10개, CSP 룰(AWS), 골든셋 4개
- `refresh` 병합 로직, `lint.py`, 빌드 게이트

### v2

- 자동 이행 검증 (`verify`)
- 규제 오버레이 (ISMS-P·개인정보보호법 우선)
- Azure/GCP 서비스 큐레이션

---

## 14. 진행 상황

### 완료

- `catalogs/data-types/classification.yaml` — 25개 데이터유형 → C/I 기여도, 수식어,
  규제 트리거 매핑
- `catalogs/data-types/availability.yaml` — RTO/RPO/가중요인 → A 유도
- `skills/deriving-security-requirements/references/profile-schema.md` — 프로파일 스키마,
  출처 분리 원칙, 인터뷰 7문항, 확인 게이트, UNDETERMINED 처리

설계 중 확정된 부가 규칙:

- **가용성은 데이터유형에서 유도할 수 없다.** C/I는 데이터 속성이지만 A는 서비스 특성이다.
  같은 회원 이메일이라도 사내 주소록이면 Low, 응급 알림이면 High. 따라서 테이블을 분리했다
- **암호화·접근통제·내부망을 감등 사유로 인정하지 않는다.** 순환논리다. 그것들은 요구사항의
  결과이지 데이터의 속성이 아니며, 감등 사유로 인정하면 도출해야 할 요구사항이 스스로를
  지운다. 반면 토큰화·가명키분리는 데이터를 실제로 경계 밖으로 내보내므로 감등이 성립한다
- **`inherit_max` 유형** — 백업·ML 학습 데이터는 원본의 최고 등급을 상속한다. 실무에서
  백업은 원본보다 통제가 약하고 노출 창이 넓다
- **`forces_requirements`** — 특정 데이터유형은 위협모델과 무관하게 요구사항을 강제
  생성한다 (`app_logs` → 로그 내 민감정보 미포함, `user_generated_content` → 업로드 검증)

### 남은 항목

- 플러그인 이름 (NIST 미포함)
- `verification.method` enum 확정 (`iac_inspect` / `code_grep` / `config_api` / `manual` / ...)
- 커뮤니티 기여 가이드 (서비스 yaml PR 템플릿·검토 기준)
- SKILL.md / 레퍼런스 문서의 언어 — 현재 한국어. 오픈소스 배포 시 기여자 접근성을 위해
  영어 전환 검토 필요 (산출물 언어와는 별개 문제)

## 15. Week 1 트레이서 불릿 — 완료

파이프라인이 end-to-end로 돈다. 결정론 계층 테스트 37개 통과.

```
프로파일 → 영향도 MODERATE → 베이스라인 287개 (AC/AU/SC 80개 번들)
        → 책임 분류: 팀 29 / 공유 20 / CSP주장 6 / 조직 25
        → 위협 교차: 교집합 8 / threat-only 3 / baseline-only 72
        → 요구사항 8개 → 병합 → 렌더 → 린트 0 error
        → 골든 재현율 100%
```

**핵심 가설 검증됨.** `threat only` 버킷에 3개가 들어왔다 — 에러리포터로의 PII
유출, 환불 웹훅 재생, 분석 식별자 재연결. 셋 다 베이스라인·스캐너 어느 쪽도
산출하지 못하는 항목이다. 이게 비었으면 설계를 되돌려야 했다.

### 트레이서 불릿이 잡아낸 버그 5개

이게 이 순서로 만든 이유다. 데이터를 다 만든 뒤였다면 전부 매몰비용이 됐다.

1. **평범한 B2B SaaS가 High 베이스라인(370개)으로 갔다.** `rpo_zero`에 가용성
   High를 준 탓. RPO(데이터 손실 허용)와 RTO(중단 허용)는 다른 질문이다. 4시간
   멈춰도 되지만 한 건도 잃으면 안 되는 서비스는 흔하다
2. **무결성 High를 남발했다.** FIPS 199에서 High는 "심각하거나 파국적"을 뜻한다.
   정산 기록 변조는 중대하지만 복구 가능 — Moderate다. High는 인증정보·비밀·
   의료기록·소스코드처럼 회복 불가능한 것들 몫. 상위수위 규칙 때문에 한 축만
   부풀어도 전체가 High로 끌려간다
3. **이용자가 KR·JP인데 GDPR이 발동했다.** 규제 트리거를 데이터 유형만으로
   판정하고 관할을 안 봤다. 오탐 하나가 나머지 findings 전체의 신뢰를 깎는다
4. **OSCAL 파라미터가 raw id로 새어나왔다.** `select` 선택지 안에 파라미터
   참조가 중첩돼 있고(AC-7), 형제 강화항목의 파라미터를 참조하는 경우도 있다
   (SC-42(2) → SC-42(1))
5. **`SC-28(3)`을 "존재하지 않는 ID" 예시로 썼는데 실재했다.** 그리고 린터가
   가짜 패밀리(`ZZ-9`)를 "아직 번들 안 된 패밀리"로 오인해 경고로만 통과시켰다.
   실재 패밀리 목록을 `meta.json`에 기록해 구분하도록 수정

버그 5개 전부 회귀 테스트로 고정했다.

### 알려진 한계 (의도적)

- 카탈로그가 AC·AU·SC 3패밀리뿐 — 나머지 207개 컨트롤은 `UNAVAILABLE`로 보고
- 큐레이션이 S3·RDS 2개뿐 — Lambda·API Gateway·Cognito는 층 기본값으로 폴백
- `threats.yaml`·`draft.json`은 손수 만든 고정 픽스처. 정상 흐름에서는 모델이
  생성한다. 결정론 체인을 모델 없이 회귀 테스트하기 위한 것
- 골든셋 1개. 설계상 4개
- CSF 2.0 crosswalk 미번들 — 요구사항의 `csf` 필드를 현재는 손으로 채운다

## 16. Week 2 — 완료

| 항목 | 결과 |
|---|---|
| 800-53 20패밀리 | 1,196개 컨트롤. Low 149 / Moderate 287 / High 370 / Privacy 96 |
| CSF 2.0 | 106 서브카테고리 / 22 카테고리 |
| ASVS 5.0 | 345개 / 17챕터 (L1 70, L2 183, L3 92), CC BY-SA 디렉토리 격리 |
| 층 매핑 | 패밀리 기본값 20개 + 컨트롤 예외 + 배포모델 5종 |
| 서비스 큐레이션 | 10개 (S3, RDS, Lambda, API GW, Cognito, DynamoDB, ECS, SQS, CloudFront, ALB) |
| 골든셋 | 4개. Low / Moderate / Moderate / High로 갈림 |
| 테스트 | 57개 |

### CSF crosswalk을 번들하지 않기로 결정

NIST는 CSF↔800-53 매핑을 OSCAL이 아니라 CPRT로 공개하는데 그 JSON 엔드포인트가
응답하지 않는다. 직접 매핑을 작성해 권위 있는 것처럼 싣는 대신, 요구사항이
작성 시점에 CSF 서브카테고리를 달고 린트가 **존재 여부만 검증**하도록 했다.
ID 무결성 보장은 유지하면서 NIST가 여기 발표하지 않은 대응관계를 지어내지 않는다.

### 층 매핑을 패밀리 기본값 구조로 바꿈

287줄을 한 줄씩 쓰는 대신 `family_defaults` + `control_overrides` +
배포모델 오버라이드 + 강화항목의 기반 컨트롤 상속. 해소 순서는 서비스 →
배포모델 → 컨트롤 예외 → 패밀리 기본값. 결과적으로 골든 4개 전부
UNDETERMINED 0이고, 예외 목록은 검토 가능한 길이로 유지된다.

### 골든셋 2~4가 잡아낸 캘리브레이션 오류 2개

**1. 소비자 앱이 전부 High로 갔다.** 원인은 `account_credentials`(c/i High).
비밀번호를 쓰는 서비스는 전부 여기 걸리니 **모든 소비자 서비스가 370개
베이스라인**이 된다. 진짜 문제는 분류였다 — FIPS 199는 시스템이 보유한
**업무 정보**로 범주화한다. 인증정보·비밀키는 업무 정보가 아니라 시스템
정보이고, 그 보호는 통제이지 영향도 입력이 아니다. `system_information: true`로
상위수위에서 제외하되 요구사항은 그대로 강제하게 했다.

**2. Low 베이스라인이 도달 불가능했다.** `audit_logs` 무결성이 고정 moderate라
감사 로그를 남기는 모든 시스템 — 즉 모든 시스템 — 이 최소 Moderate가 됐다.
감사 로그 무결성이 중요한 건 맞지만 그 의무는 AU 패밀리에서 오고 AU는 Low
베이스라인에도 있다. **분류표로 요구사항을 밀어넣은 것**이고, 이건 앞서 지적한
"High 남발" 오류의 축소판이다. `inherit_max`로 완전 파생시켰다.

두 오류 모두 회귀 테스트로 고정. 이걸로 회귀 테스트가 잡은 캘리브레이션
버그는 누적 4개다.

### 알려진 한계

- **실제 레포에서 미검증.** 프로파일 4개 전부 합성이다. 모델이 실제 코드베이스에서
  `inferred` 블록을 채우는 경로는 아직 한 번도 돌지 않았다
- `threats.yaml`·`draft.json`은 골든 1번만 존재하는 고정 픽스처. 2~4번은 모델
  실행 시 채점용
- 규제 오버레이 0개. `overlays/SCHEMA.md`만 있음
- CSP 큐레이션은 AWS만. Azure·GCP는 배포모델 층으로 폴백
- `render.py` 259줄은 프롬프트로 대체 가능 — 유지 이유는 산출물 diff 안정성뿐

### 다음 (당시 계획)

1. 실제 레포에 `/sec-req-init` 실행
2. ISMS-P 오버레이
3. Azure·GCP 서비스 큐레이션

셋 다 이후 수행했다. 아래는 그 기록이다.

---

## 17. 실전 투입과 스위프 — 도구 버그 44개

Week 2까지는 합성 프로파일만 썼다. 이후 로컬 레포 12개에 실제로 돌리고,
고친 규칙들을 값 공간에서 재검증했다. 결과를 순서대로 남긴다.

### 발견 경로별 집계

| 라운드 | 방법 | 버그 |
|---|---|---|
| 트레이서 불릿 | 최소 데이터 end-to-end 관통 | 5 |
| 골든셋 확장 | 케이스 3개 추가 | 2 |
| 실전 1회차 | `pipeline` (Spring Boot + ECS EC2) | 3 |
| 스위프 3회 | 레포 11개, 형상별 | 13 |
| 값 공간 재검증 3회 | 이미 고친 규칙 찌르기 | **21** |
| **합계** | | **44** |

### 방법론적 결론 — 이게 이 프로젝트에서 가장 값진 관찰이다

**새 레포를 여는 것보다 이미 "고쳤다"고 커밋한 규칙을 값 공간에서 찌르는 쪽이
버그 밀도가 두 배 가까이 높았다.** 레포 12개가 23개를 냈고, 재검증 3회가 21개를
냈다. 원인은 단순하다 — 각 수정에 붙인 회귀 테스트가 **내가 상상한 입력만**
담고 있었다. 규칙은 고쳤는데 그 규칙이 받는 값의 공간은 한 점만 검사했다.

가장 심한 예: no-provider 규칙을 "구조 규칙으로 교체했다"고 커밋한 다음
라운드에, `None`·`NONE`·`n/a`·`-`·`self-hosted`·`on-prem` 전부가 유령 클레임을
그대로 되살리는 것을 발견했다. 정확히 `"none"` 한 철자만 매칭하고 있었다.

부수 관찰: **수정 자체가 새 버그를 만든 사례**가 두 번 있었다(상속 풀 분리에서
인증정보 세탁, 크로싱 정규화에서 테스트 전제 오류). 골든셋 4개가 없었으면
둘 다 통과했을 것이다.

### 형상별 스위프 결과

| 레포 | 형상 | 밟은 경로 |
|---|---|---|
| `pipeline` | Spring Boot + ECS EC2 + RDS/S3/ALB/WAF | 풀 파이프라인 |
| `trading-infra` | 순수 Terraform, 앱 없음 | 빈 엔트리포인트 |
| `ai-risk-platform` | k8s + FastAPI | enum 밖 배포모델 |
| `xT-STRIDE-gpt` | 클라우드 없는 Streamlit | `csp: none` |
| `sample-ecommerce` | Next.js + Stripe + 업로드 | 규제 트리거·국외이전 |
| `aigis-kr` | pip 라이브러리 | 비서비스 형상 |
| `.pollack-ai.bak` | Azure Sentinel SOC + LangGraph | 첫 Azure, 첫 k8s |
| `mbcra-uav` | 물리 UAV | 안전필수·물리통제 |
| `jccompany-gitops` | 8파일 GitOps | 단일축 통지 |
| `llm-wiki-site` | 정적 공개 사이트 | `intended_public` |
| `IaC` | S3/GCS 어댑터 | 멀티 CSP |
| `pro-prowler` | 고객 IaC를 받는 스캐너 | 제3자 데이터 보관 |

**새 형상 하나가 버그 1개 이상을 냈다.** 예외 없었다.

### 구조가 바뀐 것

스위프가 단순 수정으로 안 끝나고 구조를 바꾼 게 다섯 개다.

- **`scripts/profile_schema.py` 신설.** 값 공간 재검증에서 나온 9개가 전부
  "사용자 문자열을 정확 일치로 비교" 한 부류였다. 호출부마다 `.lower()`를
  뿌리면 다섯을 고치고 여섯째를 남긴다. 정규화를 한 곳으로 올려 부류 자체를
  도달 불가로 만들었다
- **배포모델 2개 추가.** `kubernetes`(스위프 7개 중 2개가 씀), `embedded`(물리
  제품에서 PE/MA/MP 47개가 전부 조직 몫으로 가던 것)
- **`applies_when.deployment_model`.** 서비스 큐레이션이 배포 방식에 따라 다른
  답을 낼 수 있게. ECS가 Fargate 기준으로 SC-39를 CSP 격리라 단정하던 것이 계기
- **`system_information` × `inherit_max` 이중 풀.** 범주화 제외와 상속 풀을
  분리. 한쪽만 고치면 백업이 인증정보를 상위수위로 세탁한다
- **`customer_owned` modifier + modifier 강제 요구사항.** 데이터 유형표가 전부
  "보관자 시점"이라 고객 데이터를 쥔 서비스가 수탁자 의무를 하나도 못 냈다

### 가장 심각했던 버그 셋

1. **오타가 핵심 발견을 제조했다.** `threat_only`는 "베이스라인이 못 내는
   위험"이라는 도구의 존재 이유인데 정확 문자열 일치로 계산했다. `ac-3`,
   `AC-3 `, `ac-3.1`(카탈로그에 실린 형식), `ZZ-9`(없는 컨트롤) 전부가
   threat_only로 승격됐다. 진짜 공백과 오타가 구별 불가였고, 린트는 크로싱
   **뒤에** 돈다
2. **`forces_requirements`가 죽어 있었다.** 문서화해놓고 어떤 스크립트도 읽지
   않았다. 시스템 정보는 상위수위에서 제외되므로 강제 요구사항까지 버려지면
   **출력에 도달할 경로가 없다** — 시크릿 10개를 든 플랫폼이 시크릿 관리
   요구사항을 하나도 못 받았다
3. **refresh가 수용된 리스크의 status를 파괴했다.** CISO 승인 + 만료일이 붙은
   예외가 은퇴 처리에서 `retired`로 덮여, "어떤 리스크를 수용했나" 조회에서
   사라졌다. 감사 이력을 조용히 훼손하는 종류라 증상 없이 지나갔을 것이다

### 현재 상태

- 카탈로그: 800-53 20패밀리 1,196개 / CSF 2.0 106개 / ASVS 5.0 345개
- 서비스 큐레이션 11개 (AWS 10 + Azure Blob)
- 배포모델 6종, 골든셋 4개, 오버레이 1개(ISMS-P 101조항)
- 테스트 152개

### 남은 한계

- **모델 단계는 여전히 골든셋으로만 검증된다.** 결정론 계층은 152개 테스트가
  덮지만, 위협 모델링·요구사항 작문 품질은 합성 케이스 4개 채점이 전부다
- `threats.yaml`·`draft.json`은 골든 1번만 고정 픽스처
- Azure/GCP 큐레이션이 각 1개/0개. 멀티 CSP는 첫 제공자 기준임을 경고만 함
- GitOps 형상 미모델링 — 레포가 컨트롤 플레인인 경우를 프로파일이 표현 못 함
- 오버레이 1개. HIPAA가 다음 후보(SP 800-66이 크로스워크를 발표해 자체 작성
  불필요한 첫 사례)
